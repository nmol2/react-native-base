import React, { FunctionComponent } from 'react';
import { Text, View } from 'react-native';

interface DashboardScreenProps {}

const DashboardScreen: FunctionComponent<DashboardScreenProps> = () => {
  return (
    <View>
      <Text>Dashboard</Text>
    </View>
  );
};

export default DashboardScreen;
